# Core Airflow & Framework (Pin these major versions)
apache-airflow==2.7.1
apache-airflow-providers-common-sql==1.7.2
apache-airflow-providers-ftp==3.5.2
apache-airflow-providers-http==4.5.2
apache-airflow-providers-imap==3.3.2
apache-airflow-providers-sqlite==3.4.3

# Azure Data Lake and Storage (Critical for your data, pin these)
adlfs==2023.9.0
azure-storage-blob==12.18.2
azure-datalake-store==0.0.53
azure-identity==1.14.0
azure-core~=1.29.0

# Key Data Processing Libraries (Important, use compatible release)
pandas~=2.1.0
numpy~=1.26.0
sqlalchemy~=1.4.0

# HTTP/API Clients (Allow for minor patches)
requests~=2.31.0
aiohttp~=3.8.0
httpx~=0.25.0

# Utilities (Allow for minor patches)
python-dateutil~=2.8.0
python-slugify~=8.0.0
beautifulsoup4~=4.12.0

# Optional: If you know you use these for specific DAGs, uncomment
# fsspec~=2023.9.0
# geopy~=2.4.0
# graphviz~=0.20.0